language: python
python:
  - "2.7_with_system_site_packages"
before_install:
  - sudo apt-get update
  - sudo apt-get install python-numpy python-scipy
install:
  - pip install tox
script:
  - tox -r
env:
  - TOXENV=py27
  - TOXENV=style
  - TOXENV=coverage
